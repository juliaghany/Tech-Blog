<h1>Homepage handlebars</h1>
<div>
    {{#if blogs.length}}
    {{#each blogs as |blog|}}
    <a href="blog/{{blog.id}}">
        <h1>{{blog.blog_title}}</h1>
    </a>
    <p>{{blog.blog_description}}</p>
    {{/each}}
    {{else}}
    <h1>No blogs to show</h1>

    {{/if}}
</div>