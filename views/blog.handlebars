<h1>Single blog</h1>

<div>
    <h2>{{blog.blog_title}}</h2>
    <h3>Posted by {{blog.user.username}} on {{format_date blog.date_created}}</h3>
    <p>{{blog.blog_description}}</p>
    {{#if can_edit}}
    <form class="blog-form" style="display: none">
        <label>Title:
    
            <input type="text" id="blog-title" value="{{blog.blog_title}}">
        </label>
        <label>
            Content:
            <textarea type="text" id="blog-description" defaultValue="{{blog.blog_description}}">{{blog.blog_description}}</textarea>
        </label>
            <input type="submit">
    </form>
    <button id="edit-btn">Edit</button>
    <button id="delete-btn">Delete</button>
    <script src="/js/edit-blog.js"></script>
    {{/if}}
</div>
